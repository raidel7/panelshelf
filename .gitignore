.DS_Store
.claude/settings.local.json

# Node / server
node_modules/
npm-debug.log*

# Build output
build/
dist/

# Runtime data: extracted covers, thumbnails, and scratch space
data/

# The iPad client lives in its own repository. Xcode will recreate this path
# if it still has the old workspace open, and it must never be committed here.
ios/

# Brainstorming companion
.superpowers/
